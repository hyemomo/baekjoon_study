const finding_target = 2;
const finding_numbers = [0, 3, 5, 6, 1, 2, 4];
const is_exist_target_number_binary = (target, array) => {
// ❌무작위로 정렬되어있는 배열에서는 이진 탐색을 사용할 수 없다!!
};

const result = is_exist_target_number_binary(finding_target, finding_numbers);
console.log(result)